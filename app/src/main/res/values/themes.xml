<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MoviePoster" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">#000000</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">#000000</item>

        <!-- Custom Button Style -->
    </style>

    <style name="Theme.MoviePoster" parent="Base.Theme.MoviePoster" />

    <!-- Custom Button Style Definition -->
    <style name="WatchlistButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/button_background</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>
